<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head') %>
<body class="container-fluid">
    <h1> <%= pelicula.title %> </h1>
    <ul>
        <li> Premios: <%= pelicula.awards %> </li>
        <li>
            <% if (pelicula.length != null) { %>
                Duración: <%= pelicula.length %> minutos
            <% } else { %>
                Duración: Desconocida
            <% } %>
        </li>
        <li> Fecha de Estreno: <%= pelicula.release_date_formatted %> </li>
    </ul>
    <a href="/movies/edit/<%= pelicula.id %>">EDITAR</a>
    <a href="/movies/delete/<%= pelicula.id %>">ELIMINAR</a><br>
    <a href="/movies">Volver</a>
    <%- include('./partials/scriptsBootstrap') %>
</body>
</html>