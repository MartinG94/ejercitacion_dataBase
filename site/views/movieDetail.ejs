<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head') %>
<body class="container-fluid">
    <h1> <%= pelicula.title %> </h1>
    <ul>
        <li> Premios: <%= pelicula.awards %> </li>
        <li>
            <% if (pelicula.length != null) { %>
                Duración: <%= pelicula.length %> minutos
            <% } else { %>
                Duración: Desconocida
            <% } %>
        </li>
        <li> Fecha de Estreno: <%= pelicula.release_date %> </li>
    </ul>
    <a href="/movies/detail/<%= pelicula.id %>/editar">EDITAR</a>
    <a href="/movies/detail/<%= pelicula.id %>/eliminar">ELIMINAR</a><br>
    <a href="/movies">Volver</a>
    <%- include('./partials/scriptsBootstrap') %>
</body>
</html>