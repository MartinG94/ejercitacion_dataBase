<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head') %>
<body class="container-fluid">
    <h1>Listado de Actores</h1>
    <ul>
        <% actores.forEach( (actor) => { %>
            <li>
                <a href="/actors/detail/<%= actor.id %>"> <%= actor.first_name %> <%= actor.last_name %> </a><br><br>
            </li>
        <% }) %>
    </ul>
    <%- include('./partials/scriptsBootstrap') %>
</body>
</html>